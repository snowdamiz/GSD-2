# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | scope | Web launch entrypoint | `gsd --web` opens browser mode for the current cwd and does not launch the TUI | The user explicitly wants a browser-first flow without TUI fallback | No |
| D002 | M001 | pattern | UI contract for M001 | Preserve the exact existing skin now housed in `web/` and wire it into live GSD data/actions instead of redesigning it | Keeps scope on integration and preserves the intended experience | Yes - after parity exists |
| D003 | M001 | launchability | First-run gating | Required credentials must be entered and validated before the web workspace is usable; optional ones remain skippable | Ensures the browser path is operationally usable on first launch | No |
| D004 | M001 | pattern | Mid-run user prompts in web mode | Use a focused panel/surface for confirmations, choices, text input, and editor-style interruptions instead of burying them in modals | Matches the preferred interruption model and keeps the live workflow visible | Yes - if live testing shows a better interaction pattern |
| D005 | M001 | arch | Framework migration timing | Keep the existing Next.js skin for M001 and revisit a React-only host only after the live bridge proves whether a Next server is earning its weight | Avoids churn before the real integration exposes actual runtime needs | Yes - if the integrated web mode shows Next is just overhead |
| D006 | M001/S01 | arch | Initial web host/bridge contract | Run `gsd --web` through the existing Next.js shell as a same-origin local host backed by one long-lived bridge service, with `GET /api/boot`, `POST /api/session/command`, and `GET /api/session/events` as the first browser contract | Reuses real workspace/RPC contracts, preserves the exact skin, and avoids route-local process lifecycle bugs while proving the integration seam | Yes - if real runtime testing shows the Next host is unstable or unnecessary |
| D007 | M001/S01 | launchability | Web host bootstrap resolution | Resolve `gsd --web` to a staged standalone host at `dist/web/standalone/server.js` when packaged, with repo-only fallback to the source `web/` dev host, and pass the project cwd/session scope via `GSD_WEB_*` env vars | Gives browser mode a concrete packaged path now while keeping local development unblocked and preserving current-project scope for the later bridge service | Yes - if the packaged host format changes or the standalone build proves too heavy |
| D008 | M001/S01 | bridge lifecycle | Web bridge subprocess ownership | Start one project-scoped RPC subprocess per host runtime, resume from the project session dir with `--continue --session-dir`, and surface its boot/session/error state through the singleton bridge plus `/api/boot`/SSE snapshots | Keeps browser routes stateless, preserves current-project session continuity, and makes bridge failures inspectable without leaking session text or secrets | Yes - if the runtime host later needs a different transport than the package RPC CLI |
| D009 | M001/S01 | bridge runtime | RPC subprocess entrypoint in repo checkouts | Prefer the root GSD loader (`src/loader.ts` in source, `dist/loader.js` when packaged) for the web bridge RPC subprocess instead of the raw `pi-coding-agent` CLI | The GSD loader carries the repo/runtime defaults, resource sync, and model bootstrap behavior the browser bridge needs under `~/.gsd/agent` during local web-mode runs | Yes - if packaged installs prove the lower-level CLI is self-contained enough later |
| D010 | M001/S02 | arch | Browser onboarding contract | Use a shared onboarding service backed by runtime auth truth plus same-origin onboarding routes, a server-side session gate, and explicit bridge auth refresh while keeping launch readiness independent from onboarding completion | This keeps setup state authoritative, makes the lock enforceable, and preserves the S01 launch contract while the browser completes first-run setup | Yes - if later slices can unify onboarding and generic UI-request handling without reintroducing auth-state drift |
| D011 | M001/S02/T01 | contract | Boot onboarding compatibility | Keep `onboardingNeeded` in `/api/boot` only as a derived compatibility flag while the structured `onboarding` object becomes the authoritative state consumed by new browser/setup flows | Lets the existing shell/store keep building during S02 while stopping new server logic from depending on a lossy boolean seam | Yes - remove the shim once all consumers read the structured contract |
| D012 | M001/S02/T02 | bridge/auth | Post-setup auth sync | Treat browser credential setup as incomplete until the web bridge has restarted onto the new auth view, and expose the resulting `lockReason` plus `bridgeAuthRefresh` phase/error in onboarding state and blocked command responses | Prevents the first prompt from hitting stale auth, closes direct command bypasses, and makes refresh failures inspectable instead of looking like generic setup lock | Yes - if a future RPC-level reload command can replace the controlled restart without losing observability |
| D013 | M001/S02 | web host runtime | Standalone onboarding auth dependencies | Keep the web onboarding runtime on narrow web-local auth shims/adapters instead of dynamic file-URL imports or broad package entrypoints | The Next standalone host broke on dynamic `import(new URL(...).href)` and on broad package imports that pulled native-only modules into the server bundle | Yes - if package exports are later narrowed enough to make the shared auth storage safely bundleable |
| D014 | M001/S02 | web host runtime | Packaged boot stability | Use a node-only env-key shim for the web host and cache repeated workspace-index reads in boot | The packaged standalone onboarding path was failing on the shared dynamic-import env-key helper and warm `/api/boot` reads were too slow/flaky for fresh-profile browser startup | Yes - revisit when S04/S06 need explicit cache invalidation or a narrower shared server-only export |
| D015 | M001/S03/T01 | store | Extension UI request routing | Blocking methods (select, confirm, input, editor) queue in `pendingUiRequests` for focused-panel rendering; fire-and-forget methods (notify, setStatus, setWidget, setTitle, set_editor_text) update state maps directly without queuing | Matches the RPC protocol's blocking/non-blocking distinction and gives T02's focused panel a clean ordered queue to render from while keeping status/widget surfaces independently addressable | Yes - if future extension methods need different queuing behavior |
| D016 | M001/S03/T01 | bridge | UI response transport | All UI responses (`respondToUiRequest`, `dismissUiRequest`, `sendSteer`, `sendAbort`) route through the same `/api/session/command` endpoint used by `sendCommand` rather than dedicated endpoints | Reuses the existing command route and `commandInFlight` tracking, avoids multiplying bridge surface area before the transport contract is proven | Yes - if response-specific error handling needs finer-grained routes |
| D017 | M001/S03/T02 | UI | Terminal steer mode | Steer is a local ephemeral toggle in the terminal component (not store state) that resets when streaming stops - the input mode auto-switches between prompt/follow_up/steer based on session state and the toggle | Steer is a transient input context, not a persistent mode; keeping it local avoids store clutter and makes the toggle self-cleaning | Yes - if steer needs to survive across component remounts |
| D018 | M001/S05/T01 | pattern | Pure derivation function for workflow controls | Extract `deriveWorkflowAction` as a plain TS module (`web/lib/workflow-actions.ts`) with no React dependency, consumed by both dashboard and sidebar | Enables contract testing without DOM, reuse across multiple UI surfaces, and clear separation of action logic from rendering | Yes - if workflow action logic grows complex enough to warrant a store-level selector |
| D019 | M001/S07/T02 | web-mode launch | Remove resourceLoader.reload() from launchWebMode | Only call `initResources()` (filesystem sync) in the parent launcher process; skip `buildResourceLoader()` + `reload()` entirely | The reload() loaded 270+ extensions (~25s) in a process that exits immediately after spawning the detached server; the server manages its own resource loading | No - the parent launcher doesn't need in-memory extensions |
| D020 | M001/S07/T02 | test cleanup | lsof filter for port-based server cleanup | Use `lsof -ti :PORT -sTCP:LISTEN` not `lsof -ti tcp:PORT` when killing the server after tests | `-ti tcp:PORT` returns PIDs of ALL processes with TCP connections to that port including the test process's own client sockets; sending SIGTERM to the test process kills the worker and Node v25 reports it as a file-level `'test failed'` rather than a named test failure | No - always use -sTCP:LISTEN filter when killing server processes in integration tests |
| D021 | M002 | parity strategy | Safe browser built-in command handling comes first | Start M002 by dispatching authoritative slash commands from the web shell using `slash-commands.ts` and the existing RPC command types; known built-ins must execute, open a browser surface, or reject clearly, never fall through to model prompt text | The highest-risk M002 gap is unsafe command fallthrough, and much of the needed transport already exists, so the first slice should prove the browser can safely own the command surface | Yes - if the command registry later moves behind a shared abstraction |
| D022 | M002 | freshness strategy | Treat `/api/boot` as startup snapshot, not live feed | Keep `/api/boot` as a startup/snapshot contract and deliver workspace, auto, validation, and recovery freshness through targeted bridge/SSE view-model updates plus explicit invalidation rather than aggressive boot polling | The boot path is heavy and cached; targeted live updates protect responsiveness and reduce drift between streaming terminal state and surrounding workspace panels | Yes - if later measurements justify a different host/state transport |
| D023 | M002 | scope | Keep M002 current-project and daily-use focused | M002 stays current-project scoped and closes the browser parity needed for daily use; cross-project launcher behavior, deep analytics, and convenience-only commands remain deferred unless real usage proves they block everyday browser work | This keeps the milestone aligned with R011 and avoids accidentally widening parity work into R020/R021 scope creep before the core browser-first path is hardened | Yes - if live usage shows a deferred surface is actually required for daily work |
| D024 | M002/S01 | command routing | Browser slash-command dispatch contract | Use one shared browser dispatcher with authoritative outcomes `rpc`, `surface`, `local`, and `reject` for built-ins while preserving registered non-built-in slash commands (including GSD extension commands) on the supported prompt/extension path | This closes the unsafe built-in fallthrough risk without breaking `/gsd`, `/worktree`, `/wt`, `/kill`, or `/exit`, and keeps typed and clicked browser controls on one inspectable contract | Yes - if command handling later moves behind a broader shared slash-command registry/service |
| D025 | M002/S01/T02 | auth/logout | Browser logout only removes auth-file credentials and must fail explicitly for environment/runtime-backed auth | Reject browser `/logout` attempts when the active provider is only configured via environment/runtime auth, while allowing stored auth-file credentials to be removed and immediately refreshing the bridge auth view | The browser can reliably mutate persisted auth it owns, but it cannot unset env/runtime credentials; failing clearly preserves inspectability and avoids pretending logout completed when the provider will stay active | Yes - if a future runtime adds a safe way to revoke or mask non-file credentials from browser mode |
| D026 | M002/S01/T03 | session command surface | Group `/resume`, `/fork`, `/session`, `/export`, and `/compact` into one shared browser command surface state and reuse the same store actions from dashboard clicks and typed slash input | Session-oriented browser controls now share one inspectable surface contract (`resume`/`fork`/`session`/`compact` sections) while dashboard `New Session` reuses `submitInput("/new")` and dashboard session switches reuse the same session action method as the surface | This keeps the remaining daily-use built-ins on one observable path, avoids click-vs-slash drift, and makes session/fork/compact failures diagnosable from the same state object | Yes - if later slices split session controls into smaller dedicated surfaces without reintroducing divergent behavior |
| D027 | M002/S02 | session parity contract | Add a dedicated current-project session-browser and rename contract instead of widening `/api/boot` | Use on-demand same-origin session-browser/session-manage routes derived from `SessionManager.list(...)` and bridge-aware rename behavior for active sessions | Rich session browse/search/thread/name parity needs fields that boot intentionally omits; a narrow current-project contract preserves D022 while keeping active-session rename synchronized with the live bridge | Yes - if later state transport changes make the dedicated routes unnecessary |
| D028 | M002/S02/T01 | browser session transport | Keep session search/threading server-side and call the authoritative session-manager through narrow child-process helpers instead of importing TUI/package runtime into the web host | `/api/session/browser` returns flattened current-project thread metadata plus visible fields (`firstMessage`, parent/depth markers, active flag) while keeping the full search corpus on the server, and inactive rename reuses `SessionManager.open(...).appendSessionInfo(...)` in a subprocess | This preserves TUI session semantics, avoids widening `/api/boot`, and keeps the Next host build free of native TUI/package dependencies | Yes - if the web host later gains a cleaner shared server-only session library |
| D029 | M002/S02/T02 | browser session state | Overlay the live active-session name into boot/session-browser state in the browser store instead of widening the server contracts | The shared browser store now patches `boot.resumableSessions` and `commandSurface.sessionBrowser` from bridge session state after resume/rename so active-session `/name` updates stay visible immediately even though active rename is RPC-only and does not rewrite the session file | This keeps D022's lightweight boot/on-demand route boundaries intact while preserving browser-visible parity for active-session rename/resume flows | Yes - if a later server contract can surface active-session rename state directly without widening `/api/boot` |
| D030 | M002/S02/T03 | settings parity | Represent browser settings mutations as structured `commandSurface.settingsRequests` state while deriving live retry/compaction truth from bridge `get_state` plus event-triggered refresh | Queue/follow-up/auto-compaction/auto-retry/abort-retry now expose per-control pending/result/error state in the shared store, and the bridge refreshes its session snapshot on retry/compaction lifecycle events so the browser reads authoritative live state instead of inferring from terminal text | This keeps the shared command surface inspectable for agents, preserves D022's narrow boot contract, and avoids browser-local guesses for retry/compaction activity | Yes - if a later shared state transport can replace the current bridge snapshot/update pattern |
| D031 | M002/S02/T04 | git parity | Keep browser Git parity on an on-demand `/api/git` route backed by current-project repo truth instead of widening `/api/boot` or importing the full git service into the web host | The sidebar Git affordance now opens a shared `git` command-surface section that loads branch/main/status counts from native helpers plus raw porcelain parsing, with explicit `not_repo` and load-error visibility, while the route uses local read-only git exec helpers to stay bundle-safe | This removes the dead control, preserves D022's lightweight boot contract, keeps current-project scoping explicit, and avoids pulling non-web-safe git-service dependencies into the Next route bundle | Yes - if a future shared server-only git library replaces the local helper seam |
| D032 | M002/S02/T05 | shell signal lifecycle | Treat empty `setTitle` updates as clear operations and consume `editorTextBuffer` through an explicit store action while rendering widgets by placement around the browser editor | The browser shell now clears blank title overrides instead of leaving empty chrome, consumes editor prefills exactly once to avoid infinite input replay, and keeps extension widgets bounded and placement-aware without widening server contracts | This makes title/widget/editor extension signals visible and inspectable in browser mode while preserving the lightweight boot/on-demand transport boundaries established earlier in S02 | Yes - if a later shared shell-state abstraction subsumes the local render helpers and consume-once action |
| D033 | M002/S03 | live state transport | Reuse the existing bridge/SSE/store spine for freshness by emitting targeted live-state invalidation payloads, while keeping `/api/boot` as a snapshot and limiting reconnect/visibility recovery to one soft boot refresh | Cheap, frequent freshness belongs on the existing event stream and store, but broad boot polling would reintroduce cost and drift; one soft boot refresh remains the coarse recovery path after disconnect or tab return | This preserves D022's snapshot boundary, gives the browser explicit invalidation semantics, and avoids token-by-token or whole-boot churn during live work | Yes - if later measurements justify a different transport |
| D034 | M002/S03 | recovery diagnostics surface | Keep heavy doctor/forensics truth on an on-demand current-project recovery route and render it through the shared browser command surface rather than streaming it or widening `/api/boot` | Recovery diagnostics are valuable but expensive and verbose; the browser needs a named inspectable surface with actionable controls, not a larger startup payload or noisy SSE feed | This reuses the inspectable browser surface established in S01-S02, keeps build-sensitive diagnostics behind a narrow helper seam, and makes failures actionable without leaking raw session text | Yes - if a later browser-specific diagnostics shell replaces the shared surface |
| D035 | M002/S03/T01 | freshness contract | Live browser freshness invalidation transport | Emit explicit `live_state_invalidation` bridge/SSE events keyed by reason, source, and freshness domains, and only bust the workspace-index cache on `agent_end` while retry/compaction and session mutations emit narrower cues | This keeps `/api/boot` snapshot-shaped, gives T02 a typed inspectable freshness contract, and avoids invalidating expensive workspace state on every live signal | Yes - if later measurements show direct live-state payloads are cheaper than invalidation-driven refreshes |
| D036 | M002/S03/T02 | selective refresh transport | Browser-targeted live state reload route | Add a same-origin `/api/live-state` route for narrow `auto`, `workspace`, and `resumable_sessions` reloads, while keeping reconnect/visibility recovery on one soft `/api/boot` refresh | T02 needs targeted browser freshness without widening `/api/boot` or importing server helpers directly into the client store | Yes - if a later shared transport can replace the route without reintroducing broad boot polling |
| D037 | M002/S03/T03 | recovery diagnostics transport | On-demand recovery diagnostics route plus typed browser action ids | Shape current-project doctor, validation, interrupted-run, and bridge/auth retry state behind `/api/recovery`, and return browser-native action ids/labels so the client can refresh or jump to authoritative retry/resume/auth controls without parsing transcript text | Keeps heavy diagnostics off `/api/boot` and SSE, preserves current-project scoping/redaction, and gives the browser an inspectable action contract instead of brittle text heuristics | Yes - if a later diagnostics shell replaces the shared command surface |
| D038 | M002/S04 | runtime proof | Final browser hardening proof harness | Reuse one shared packaged-host Playwright harness with configurable launch cwd and seeded temp-project fixtures for S04 instead of inventing a second runtime stack or relying on route-only mocks | S04 is proof-first; the remaining risk is assembled browser behavior under real launch, refresh/reopen, and interrupted-run recovery, and one harness keeps repo-root and fixture-project proof on the same runtime seam | Yes - if a later shared runtime test library supersedes the slice-local harness |
| D040 | M003 | merge strategy | Single merge of upstream/main | Merge all 398 upstream commits in one pass rather than staging by version tag | The conflicts are concentrated in ~50 files regardless of staging; one merge is simpler and avoids repeated conflict resolution on the same files | No |
| D041 | M003 | parity scope | Absolute browser parity for all /gsd subcommands | Every /gsd subcommand gets a browser-native surface — no exceptions, no "TUI-only" commands | The user explicitly requested absolute parity, not selective coverage | No |
| D042 | M003 | visualizer shape | Dedicated page with tabbed sections | Build the workflow visualizer as a new dedicated page with tabbed sections (not an overlay or embedded in dashboard) | Matches the user's preference for a standalone visualization surface rather than mimicking the TUI overlay pattern | Yes — if user feedback shows a different layout works better |
| D043 | M003 | diagnostics shape | Separate panels for forensics, doctor, skill-health | Keep diagnostics surfaces as separate panels rather than unifying into one diagnostics page | Each has different data shapes, actions, and use patterns; separate surfaces keep them focused | Yes — if usage shows they should be combined |
| D044 | M003 | knowledge/captures shape | Combined dedicated page | Knowledge and captures/triage share one dedicated page since both are project-context artifacts | Thematically related and the user confirmed combining makes sense | Yes — if they grow complex enough to warrant separation |
| D045 | M003 | settings approach | Extend existing command surface | Add model routing, provider management, and budget visibility to the existing settings command surface rather than building a new settings page | Preserves the established command-surface pattern from M002 | Yes — if the settings surface grows too crowded |
| D046 | M003-S01-T02 | architecture | How to handle fork's per-module cache clears (clearParseCache, clearPathCache) vs upstream's centralized invalidateAllCaches | Take upstream's centralized invalidateAllCaches() from cache.ts; do not re-add individual cache clear imports | Upstream consolidated per-module cache clears (clearParseCache from files.ts, clearPathCache from paths.ts, invalidateStateCache from state.ts) into a centralized invalidateAllCaches() exported from cache.ts. The individual functions may no longer be exported. Fork's web code that references individual cache clears will need updating in T04 to use the new centralized API. | Yes |
| D047 | M003-S01-T02 | architecture | Whether to re-add fork's web-mode additions to GSD extension core modules after taking upstream base | Take upstream for all 7 GSD extension core modules without re-adding fork-specific code | Investigation found no web code imports from any of the 7 conflicted GSD modules (auto.ts, index.ts, commands.ts, state.ts, preferences.ts, types.ts, git-service.ts). Web code only imports from native-git-bridge.ts. Fork's additions were internal optimizations (batch parsing, individual cache clears) that upstream independently implemented or superseded with better versions (DB-first content loading, centralized cache invalidation, debug instrumentation). No fork-only web types, web bridge hooks, or web exports were present. | Yes |
| D048 | M003/S01/T03 | architecture | How to merge fork's web-mode CLI routing into upstream's cli.ts | Keep upstream's top-level procedural structure, add web-mode routing as early-exit checks before interactive mode. Import parseCliArgs, runWebCliBranch, migrateLegacyFlatSessions from fork's cli-web-branch.ts module. Replace inline session migration with getProjectSessionsDir from project-sessions.ts. Drop fork's runCli() export and CliDeps DI pattern (nothing else imports runCli). | Upstream's procedural top-level code is simpler and matches their design intent (no DI needed since cli.ts is always the entry point). Fork's web-mode additions live in separate modules (cli-web-branch.ts, web-mode.ts, project-sessions.ts) that are NOT conflicted, so only the import sites in cli.ts needed rewiring. Preserving upstream's structure minimizes future merge conflicts. | Yes |
| D049 | M003/S01/T03 | architecture | Whether to re-add fork-specific code to the 5 GSD extension modules | Take upstream for all 5 extension modules (files.ts, activity-log.ts, dashboard-overlay.ts, guided-flow.ts, worktree-manager.ts) with no fork re-additions needed. | Diffing fork vs upstream for each file showed: files.ts — upstream added native parser fast-paths, fork had nothing; activity-log.ts — upstream optimized memory/IO (#611), fork had older code; dashboard-overlay.ts — upstream added disposed guard, fork had nothing; guided-flow.ts — upstream added pendingAutoStart and workingDirectory, fork had nothing; worktree-manager.ts — upstream replaced execSync with native git bridge, fork had older shell-out code. No web-specific code existed in any of these files. | Yes |
| D050 | M003/S01/T04 | build | How to handle fork file web-runtime-oauth.ts that imported from ../dist/oauth.js (circular build dependency) | Change import to ./oauth.js (source-relative) instead of ../dist/oauth.js — eliminates circular dependency on pre-built dist/ artifacts | The file is compiled as part of src/ and oauth.ts is a sibling in the same directory. Importing from dist/ creates a chicken-and-egg problem where dist must exist before the build that creates dist. Source-relative imports work correctly since tsconfig includes src/**/*.ts. | Yes |
| D051 | M003/S02 | architecture | How to route /gsd subcommands in browser dispatch | Add dispatchGSDSubcommand() function called when parsed.name === "gsd". Classify subcommands into surface (20), bridge passthrough (9), and inline help (1). Surface names use "gsd-" prefix to distinguish from built-in surfaces. Single generic CommandSurfaceTarget { kind: "gsd" } for all GSD surfaces. | Extends the existing dispatch architecture (BrowserSlashCommandSurface union, SURFACE_COMMANDS map, command-surface-contract.ts) rather than creating a parallel system. The "gsd-" prefix prevents collision with built-in surfaces (e.g., /export vs /gsd export). A single generic target kind avoids 20 target variants for surfaces that will be replaced with real content in S04-S07. Passthrough commands continue reaching the bridge unchanged. Research confirmed this classification covers all 30 registered subcommands. | Yes — target shape will be refined when S04-S07 build real surface content |
| D052 | M003/S03 | architecture | How visualizer Export tab generates downloads | Client-side blob downloads using URL.createObjectURL() — no server-side export endpoint | The VisualizerData is already fully loaded in the browser from the main /api/visualizer fetch. Generating markdown/JSON from that data and triggering a download is trivial client-side and avoids creating a second API route. The TUI writes to disk (writeExportFile), but the browser equivalent is a file download, not a server write. | Yes — if exports need server-side computation or disk writes |
| D053 | M003/S03 | architecture | How /gsd visualize dispatch navigates to visualizer view | Add "view-navigate" dispatch result kind that emits gsd:navigate-view CustomEvent to switch app-shell activeView, rather than opening command surface | The visualizer is a full app-shell view (like dashboard/roadmap), not a command surface panel. Opening it in the command surface would be inconsistent — it belongs in the main content area. The gsd:navigate-view event pattern parallels the existing gsd:open-file event for cross-component navigation. This pattern is reusable for future command→view navigations (e.g., S04-S07 if any become full views). | Yes — if command surfaces prove to be better containers for some views |
